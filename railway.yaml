services:
  - name: spring-backend
    type: web
    runtime: docker
    plan: free
    region: US East (Virginia, USA)
    dockerfilePath: Dockerfile
    dockerContext: .
    healthCheckPath: /health
    autoDeploy: true